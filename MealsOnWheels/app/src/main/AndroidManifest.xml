<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mealsonwheels">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VendorEditItem"></activity>
        <activity android:name=".deliveryHomePage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".restrauntPage"></activity>
        <activity android:name=".userSignup" />
        <activity
            android:name=".userHomePage"
            android:label="@string/title_activity_user_home_page" />
        <activity android:name=".vendorHomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>